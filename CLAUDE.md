# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Обов'язок підтримувати цей файл

**Кожного разу, коли вносиш зміну в архітектуру, флоу, env-змінні, схему БД, job-типи чи розклад — онови відповідний розділ цього файлу в тому ж дифі.** Це не окрема задача "колись потім", а частина самої зміни. Якщо додав нову таблицю/колонку — онови "Схема БД". Якщо змінив логіку — онови "Архітектура" і "Env-змінні". Якщо переніс щось зі статусу "заплановано" в "зроблено" — онови "Поточний статус". Мета — щоб наступний інстанс Claude Code міг зрозуміти реальний стан проєкту з цього файлу, не перечитуючи всю історію чату.

## Що це за проєкт

Автоматизація для клієнта (СТО/автосервіс): дзвінки менеджерів через Binotel (АТС) автоматично транскрибуються через OpenAI, класифікуються (успішність / найслабший етап / оцінка) і **зберігаються в Postgres із прив'язкою до конкретного менеджера**; окремий Telegram-бот читає ту саму БД і віддає звіти/статистику/архів. Раніше було на n8n; переписано на чистий Node.js, деплой — два Render-сервіси (cron-інжест + persistent-бот).

**Два компоненти (монорепо):**
1. **Інжест** (`src/jobs/`, Render Cron Job) — збір, транскрипція, класифікація і атрибуція дзвінків у БД. Звітів тут немає свідомо.
2. **Бот звітності** (`src/bot/`, Render Background Worker) — інтерактивний Telegram-бот (grammy, long-polling): авто-звіти, статистика менеджера, архів розмов, база знань (RAG). Читає ту саму БД. **Той самий єдиний бот** `@OBVCarServiceWork_bot`, що й алерти інжесту.

Раніше в інжесті були PDF-звіти в Telegram + Google Sheets — видалено. Google Sheets вирізано повністю; PDF-звіти повернулись, але вже в **боті** (`src/bot/pdfReport.js`, `pdfkit`), а не в інжесті.

Пріоритети клієнта, які визначили архітектурні рішення: мінімізація регулярних витрат (усе на безкоштовних/копійчаних тарифах, де можливо) і надійність (жодні дані не повинні губитись мовчки).

## Команди

```
npm install

npm run poll                              # інжест: обробити нові дзвінки з моменту чекпоінта + ретрай pending-черги
npm run bot                               # запустити Telegram-бот звітності (long-polling; треба TELEGRAM_BOT_TOKEN)
npm run test:call -- <generalCallID>      # смок-тест одного дзвінка: Binotel record → OpenAI transcribe, без БД
npm run backfill -- "2026-07-01 00:00:00" "2026-07-03 23:59:59"   # обробити довільний історичний період
npm run reattribute:shared                # переприв'язати старі 901/902 дзвінки до людей (AI по збережених транскриптах)
```

Тестів і лінтера в проєкті немає. Перевірка коректності — `node --check src/*.js` (синтаксис) + реальний прогін проти живих Binotel/OpenAI/Neon креденшлів з `.env` (усе покрито живими тестами по ходу розробки, автоматизованого test suite нема).

`JOB_TYPE=poll` — єдиний режим `src/jobs/index.js` (те, що задеплоєно на Render). Історичний `report`-режим видалено разом зі звітністю.

Код на **ESM** (`import`/`export`, `"type": "module"` в package.json). Відносні імпорти — з розширенням `.js`. CJS-залежності підключаються через default-import (напр. `import pg from 'pg'; const { Pool } = pg;`).

## Структура репо

Монорепо, feature-based. Стандарт узгоджено під майбутній окремий бот звітності:

```
src/
  core/     — спільне ядро (використовують і jobs/, і bot/):
              store, binotel, transcribe, classifyCall, identifyManager, telegram, retry
  jobs/     — задеплоєний cron-джоб (інжест): index.js (entrypoint) + pollNewCalls + processCalls
  scripts/  — одноразові інструменти: backfill, testSingleCall, reattributeShared
  bot/      — Telegram-бот звітності (grammy, окремий токен, окремий сервіс):
              index.js (entrypoint) + keyboards + stats + archive + report + analyze + kb + time + ui
              (див. src/bot/README.md)
```

Коли пишеться нижче просто "processCalls.js", "store.js" тощо — шукати за цією мапою (`core/`, `jobs/` чи `bot/`).

## Архітектура

**Один Render Cron Job** (`binotel-poller`, `*/15 * * * *`, `JOB_TYPE=poll`) робить усе:

1. **`retryPendingCalls()`** (`processCalls.js`) — спершу ретраїть усе, що застрягло в `pending_calls` з минулих разів
2. **Поллінг** (`pollNewCalls.js`): чекпоінт (`app_state.last_polled_until`) → зараз. Не фіксоване "останні N хв" — щоб пропущений/затриманий cron-запуск не створював діру. `listCallsForPeriod` (Binotel) обмежений 24 год на запит, тому діапазон ріжеться на шматки по 23 год (`processCalls.js: splitIntoChunks`)
3. Для кожного нового дзвінка: якщо `durationSec <= 0` — пропустити (не відповіли); якщо `recordingStatus != 'uploaded'` — в `pending_calls` на ретрай; інакше — `getCallRecordUrl` → `transcribeAudio` (OpenAI, див. «Мова» нижче) → `classifyCall` (окремий легкий структурований запит: успішність / найслабший етап / оцінка 1-10) → **резолвинг імені оператора** (див. нижче) → `saveCall` в Postgres (транскрипт + класифікація + `manager_name` разом)

**Мова транскрибації (`core/transcribe.js`).** Проблема: українська розмовна з суржиком («да», «шо», «тіки») часто транскрибується як російська. Флоу: (1) транскрипція з українським prompt-ухилом; (2) окремий OpenAI-запит визначає `spoken` (реально сказана мова; суржик = **uk**) і `transcriptLanguage` (якою мовою вийшов текст); (3) якщо не збігаються (сказали uk — записало ru) → **переписуємо з примусовою `language`**. `CALL_LANGUAGE` (env) форсить мову й вимикає детекцію. Підтверджено наживо 2026-07-10: ru-дзвінок лишається ru, а uk-записаний-як-ru переписується на uk.

Ніякої звітності на кожному запуску вже немає — це кінець флоу інжесту.

### Бот звітності (`src/bot/`, окремий Render Background Worker, long-polling grammy)

Персистентний процес (не cron), стартує `bot.start()`.

**Система ролей / контроль доступу (`bot/access.js` + `bot/roles.js`, таблиця `bot_users`).** Доступ більше НЕ керується env-allowlist'ом — ролі живуть у БД. Ролі — фіксований ENUM (без «додати роль»):
- **director** та **marketer** — однаковий ПОВНИЙ доступ (admin-tier): статистика всіх, архів, база знань (питання+файли), промпт аналізу, керування ролями.
- **manager** — «📊 Мій звіт» (стата ЛИШЕ по собі + свій телефон) і база знань (питання). Ідентичність — через `bot_users.operator_name` (директор лінкує при додаванні), тож у звіт по собі природно входять і дзвінки зі спільних номерів, атрибутовані на те саме ім'я.
- **mechanic** — лише база знань (питання).

`access.js`: `getUser` (роль по `telegram_id`, кеш із `invalidateRole`), `canAccess(role, feature)` + `featureOf(ctx)` — центральний gate в auth-middleware (класифікує кожен апдейт у фічу й пускає/блокує). `mainMenu(role)` і нативні команди (`setMyCommands` зі `scope:{type:'chat'}`) підбираються під роль. **Bootstrap:** `seedDirectors()` на старті сідить user-id з `BOT_ALLOWED_CHAT_IDS` + `TELEGRAM_CHAT_ID` як директорів (щоб не залочитись), відсіюючи негативні (групові) id — ролі прив'язані до `ctx.from.id` (user), а `TELEGRAM_CHAT_ID` тут = груповий чат звітів.

**Керування ролями (`roles.js`, admin-only, блок «👥 Ролі» / `/roles`):** кнопки Маркетологи/Менеджери/Механіки (директорів UI навмисно не чіпає — захист seed-власника від видалення). Роль → список людей + «➕ Додати» → людина → «🗑 Видалити». **Додавання** (Telegram не дає ботам шукати за `@username` — це навмисне обмеження приватності): (1) `request_users` — обрати колегу з контактів, отримуємо `user_id`(+ім'я/username) МИТТЄВО, без потреби, щоб людина спершу писала боту (Bot API 7.2, grammy `Keyboard.requestUsers`); (2) поділитися контактом (є `user_id` → active, інакше pending за телефоном); (3) ввести телефон → pending. **Pending → active:** незнайомець ділиться СВОЇМ контактом на `/start`, `activatePendingByPhone` матчить за останніми 9 цифрами. Для manager після додавання — вибір оператора з roster (`getOperators`) → `operator_name`. ⚠️ Навіть маючи `user_id`, бот не може написати людині першим, поки вона не натисне Start (правило Telegram) — тож особисті авто-звіти неможливі; «Мій звіт» менеджер тягне сам інтерактивно.

**Навігація:** (1) нативні команди — кнопка «Menu» біля поля вводу, **список підбирається під роль** (`setMyCommands` зі `scope:{type:'chat'}`): admin — `/menu /stats /archive /ask /files /report /prompt /roles`; manager — `/menu /myreport /ask`; mechanic — `/menu /ask`; (2) inline-меню в повідомленнях (`keyboards.js: mainMenu(role)`) + inline «« Меню» на всіх екранах. Постійну reply-клавіатуру (`quickKeyboard`/`QUICK`) **прибрано** (забагато UI-навантаження) — `/start` шле `remove_keyboard`, щоб очистити її в старих клієнтів. Нативний список команд свідомо покриває всі пункти inline-меню (Статистика/Архів/**База знань**/**Файли**/Звіт) + `/menu` + адмінську `/prompt`. Inline-кнопки: «📊 Статистика менеджера», «🗂 Архів розмов», «📚 База знань» (`kb:ask`), «📁 Файли» (`kb:menu`), «🔄 Звіт зараз».

**Модель «активного екрана»** (`bot/ui.js: showScreen()`, замінила прямі `editMessageText`): меню/пікер завжди живе в НАЙнижчому повідомленні, щоб фокус юзера (низ чату) збігався з дією. Якщо натиснуте меню ще внизу — редагуємо на місці (плавно, без спаму); якщо його «поховало» під контентом (транскрипт / відповідь KB / текст промпту) — надсилаємо свіже меню внизу й видаляємо старе. `installMessageTracker` (API-transformer) веде id останнього надісланого ботом повідомлення на чат (щоб знати, чи меню ще внизу); `ctx.session.screenId` — id активного екрана (для видалення старого). Контентні дії (перегляд промпту тощо) після себе ре-планують меню вниз. Best practice підтверджено пошуком (стандарт — edit-in-place; тут адаптовано під контент-важкий бот).

Меню (inline-кнопки):

1. **Авто-звіти** (`report.js`) — власний планувальник (`setInterval` 30с звіряє київський час зі слотами `BOT_REPORT_TIMES`, дефолт `13:00,19:30`). **Це НЕ окремий Render-cron, а in-process інтервал усередині persistent-воркера бота** — на VPS достатньо тримати процес `node src/bot/index.js` живим (systemd/pm2). Період — з моменту попереднього авто-звіту (`app_state.last_report_slot`/`last_report_until`, дедуп слоту переживає рестарт; in-memory lock від подвійного спрацювання за хвилину). Групує дзвінки по менеджеру → OpenAI-резюме (`analyze.js`, промпт редагується через `/prompt`, див. фічу 5) → **PDF-файл** (`pdfReport.js`, `pdfkit`): одна сторінка на менеджера, заголовки-CAPS, `*жирний*`, булети, роздільники `---`. Той самий `sendReport()` (→ `api.sendDocument`) використовується і в авто-звітах, і в кнопці "Звіт зараз" (ручний за сьогодні, стан розкладу не чіпає) — формат ідентичний скрізь.
2. **Статистика менеджера** (`stats.js`) — менеджер → період (день / тиждень з пн / місяць / квартал, київські, `bot/time.js`) → к-сть/успішність/бал/слабкий етап + нотатки (`manager_notes`).
3. **Архів розмов** (`archive.js`) — менеджер → період → список (пагінація) → дзвінок: транскрипт + "🎧 Прослухати" (свіжий `getCallRecordUrl` з Binotel → `sendAudio`). У кнопці дзвінка успішність позначена 👍/👎 (не ✅/❌). У списку менеджерів біля імені показується період активності `dd.mm.yy-dd.mm.yy` (`operatorListKeyboard(..., {showDates:true})`): від найранішого обробленого дзвінка (`getOperators` → `MIN(start_time)`, проксі «коли Binotel уперше побачив ім'я» — реальної дати реєстрації Binotel API не віддає) до сьогодні. Тільки в архіві; у статистиці той самий пікер без дат.

**Аліаси імен операторів** (`bot/operators.js: displayName`) — мапа «сирий `manager_name` → людське ім'я», застосовується ЛИШЕ на рендері (кнопки, заголовки, деталі дзвінка, PDF-звіт). БД і callback-data лишаються сирими → групування/запити/атрибуція не змінюються, мапа повністю оборотна. Дефолт: номер директора `0674738200` → «Богдан» (показується як `👤 Богдан`, а не `☎️ Спільний 0674738200`). Розширюється env `OPERATOR_ALIASES` (пари `номер=Ім'я` через кому, мерджиться поверх дефолту).
4. **База знань** (`kb.js`, кнопка «📚 База знань» → `kb:ask`, команда `/ask`) — **RAG** по завантажених посібниках. Власник надсилає боту документ (PDF `unpdf` / DOCX `mammoth` / TXT) → витяг тексту → нарізка (`chunkText`, ~2400 симв. з перекриттям) → embeddings (`text-embedding-3-small`) → зберігання в Postgres (`kb_docs` + `kb_chunks` з `vector(1536)`, **pgvector**). Питання: embedding → cosine-пошук топ-6 шматків (`<=>`) → gpt-4o-mini відповідає **лише** з тих фрагментів (інакше «немає відповіді»), з посиланням на файл. Екран «📚 Файли»: кнопка на кожен файл → деталі (📄 Відкрити оригінал через збережений Telegram `file_id` / 🗑 Видалити з підтвердженням, каскад) + «➕ Завантажити новий». `migrateKb()` guard'иться: нема pgvector → KB вимкнено, решта бота живе. **Усі KB-екрани — простий текст без Markdown** (назви файлів часто містять `_`, що ламало парсинг Markdown — був баг «нічого не відбувається»). Після відповіді бот **лишається в режимі питань** (`awaiting=kb_question` не скидається) — наступне повідомлення теж трактується як питання (follow-up); вихід — `/menu`.
5. **Промпт аналізу** (`prompt.js`, команда `/prompt`) — власник переглядає / змінює / скидає системний промпт, яким AI оцінює роботу операторів у звітах. Меню: 👁 переглянути поточний, ✏️ змінити (надіслати новий текст повідомленням → `awaiting=prompt`), ↩️ скинути до стандартного. Зберігається в `app_state.analyze_prompt`; `analyze.js: getAnalyzePrompt()` повертає його або `DEFAULT_ANALYZE_PROMPT`. Змінений промпт одразу діє і в авто-звітах, і в «Звіт зараз». **`DEFAULT_ANALYZE_PROMPT` — структурований звіт під PDF** (розділи CAPS-заголовками: загальна характеристика / сильні сторони / системні помилки / найслабший етап / динаміка / топ-3 точки росту / готові формулювання; `*жирний*` для назв пунктів). Формат навмисно узгоджений із рендером `pdfReport.js` (CAPS-рядок → заголовок, `*...*` → bold, `-`/`•` → булет, `---` → роздільник; markdown нормалізується — `**подвійні**` зірочки не лишають артефактів).

Бот НЕ пише в `calls`/`pending_calls` — лише читає; пише в `manager_notes`, `kb_docs`/`kb_chunks`, `bot_users` (ролі) і ключі `app_state` (звітні + `analyze_prompt`).

### Ідентифікація оператора (`resolveManagerName` в `processCalls.js`)

**Джерело правди — Binotel, а не локальна таблиця.** Таблиці `managers` більше немає (клієнт керує операторами в Binotel). Оператор ідентифікується **іменем**, і по цьому ж імені (`manager_name`) далі групується вся статистика/архів/звіти.

- **Особистий номер** (903/904/905) → `manager_name` = `employeeData.name` від Binotel (напр. "Роман", "Владимир", "Андрей" — саме так, як пише Binotel).
- **Спільна слухавка** (номер у `SHARED_EXTENSIONS`, дефолт `901,902`) або номер без імені → Binotel не знає, хто відповів, тому оператор визначається **з транскрипту**: `identifyManager.js` окремим OpenAI-запитом обирає, ХТО зі списку відомих операторів представився. Список кандидатів (**roster**) — динамічний, `getOperatorRoster()` = distinct не-числові `manager_name` з `calls` (тобто імена, які Binotel уже давав на особистих номерах). AI обмежений `enum`'ом кандидатів, тож повертає точно одне з тих імен (сам зводить "Володя"→"Владимир") або `null`. Збіг → це ім'я. Нема збігу/імені → лишається голий номер (`901`) як мітка.

Тобто roster сам «наповнюється» з Binotel: як тільки менеджер зробив дзвінок з особистого номера, його ім'я стає кандидатом для матчингу спільних дзвінків. Жодної ручної таблиці/aliases. **Клієнт попросить операторів представлятись** на спільних — від цього залежить якість атрибуції 901/902.

`reattributeShared.js` (`npm run reattribute:shared`) — переганяє вже збережені спільні дзвінки (з голим номером) через ту саму AI-ідентифікацію по збережених транскриптах.

### Надійність (свідомі архітектурні рішення, не міняти без причини)

- **Чекпоінт замість вікна** — гарантує відсутність дір при збоях cron
- **`pending_calls`** — дзвінок, що впав на будь-якому кроці, ретраїться на кожному наступному `poll` (не втрачається), до `MAX_PENDING_ATTEMPTS` (20), потім `status='failed'` + Telegram-алерт, рядок лишається в таблиці для ручної перевірки (нічого не видаляється мовчки)
- **`withRetry`** (`retry.js`) — обгортає кожен зовнішній виклик (Binotel/OpenAI/Telegram), 2-3 спроби з наростаючою паузою
- **Telegram (інжест) — тільки алерти.** `core/telegram.js` лише `sendMessage`/`sendAlert` (вихідні), для сповіщень про збої й невідомих менеджерів. `sendMessage` ділить текст на шматки ≤4096 і має markdown→plain fallback. Шле через той самий єдиний бот `@OBVCarServiceWork_bot` (вихідні виклики не конфліктують з `getUpdates` бота звітності).

### Схема БД (Postgres/Neon, `core/store.js: migrate()`)

- **`calls`** — `general_call_id` (унікальний), `internal_number`, `manager_name` (ім'я оператора — ключ групування скрізь), `call_type`, `start_time`, `duration_sec`, `transcript`, `is_success`, `weakest_stage`, `communication_score`. (Таблиці `managers` й колонок `manager_id` більше немає — Binotel є джерелом операторів. `migrate()` містить ідемпотентне прибирання цих legacy-артефактів — `DROP COLUMN IF EXISTS manager_id` у `calls`/`manager_notes` + `DROP TABLE IF EXISTS managers`; живу Neon-БД почищено 2026-07-14, було 100% NULL, дані не втрачено.)
- **`pending_calls`** — підмножина полів calls + `attempts`, `status` (`pending`/`failed`), `last_error`
- **`manager_notes`** — `id`, `operator_name` (ім'я оператора = `calls.manager_name`), `author`, `note`, `created_at`. Нотатки, які лишає власник у боті (фіча 2)
- **`bot_users`** — контроль доступу (ролі): `id`, `telegram_id` (UNIQUE, NULL поки invite pending), `role` (director|marketer|manager|mechanic), `phone`, `username`, `display_name`, `operator_name` (лінк менеджера до `calls.manager_name` для стати по собі), `status` (active|pending), `added_by`, `created_at`. **Це НЕ воскресіння старої `managers`** (та була про атрибуцію дзвінків) — тут суто авторизація «хто і що може в боті»; Binotel лишається джерелом правди щодо того, хто говорив. Функції в `store.js`: `getBotUser`/`getBotUsersByRole`/`upsertBotUserByTelegram`/`addPendingBotUser`/`activatePendingByPhone`/`setBotUserOperator`/`setBotUserPhone`/`deleteBotUser`/`seedDirector`
- **`kb_docs`** / **`kb_chunks`** — база знань (фіча 4): `kb_docs` (файл + `file_id`/`mime` для повторної відправки оригіналу + `chunk_count`), `kb_chunks` (`doc_id` FK ON DELETE CASCADE, `ord`, `content`, `embedding vector(1536)`, hnsw-індекс cosine). Створюються окремо в `migrateKb()` (потребує `CREATE EXTENSION vector`), не в `migrate()`
- **`app_state`** — key-value: `last_polled_until` (чекпоінт інжесту); `last_report_slot`/`last_report_until` (розклад авто-звітів, пише **бот**); `analyze_prompt` (кастомний промпт аналізу, якщо власник змінив через `/prompt`; нема ключа → `DEFAULT_ANALYZE_PROMPT` з `analyze.js`). Обгортки — `store.js: getStoredAnalyzePrompt/setStoredAnalyzePrompt/clearStoredAnalyzePrompt` (як `getCheckpoint`/`getReportSlot`)

Час: інжест рахує все в UTC (чекпоінт — абсолютний момент), йому Kyiv не треба. **Боту** Kyiv потрібен (слоти звітів + межі періодів) — `src/bot/time.js` рахує через `Intl.DateTimeFormat`, без хардкод-офсету (ламається на EET/EEST). У `core/` спільного `time.js` немає — це суто ботова логіка.

## Env-змінні

Повний список і коментарі — `.env.example`. Ключове:
- `BINOTEL_API_KEY/SECRET`, `BINOTEL_BASE_URL` — ендпоінти підтверджені емпірично на реальному акаунті 2026-07-06 (`stats/list-of-calls-for-period.json` з `startTime`/`stopTime` в unix-секундах, `stats/call-record.json`)
- `OPENAI_ANALYZE_MODEL` — `classifyCall`, `identifyManager`, звіти й відповіді бази знань (дефолт `gpt-4o-mini`). `OPENAI_EMBED_MODEL` — embeddings бази знань (дефолт `text-embedding-3-small`, 1536-dim)
- `SHARED_EXTENSIONS` — comma-separated внутрішні номери спільних слухавок (дефолт `901,902`), для яких менеджер визначається з транскрипту
- `OPERATOR_ALIASES` — (бот, опційно) пари `номер=Ім'я` через кому для показу сирого `manager_name` під людським іменем (напр. `0674738200=Богдан`). Мерджиться поверх дефолту в `bot/operators.js` (де `0674738200=Богдан` уже прошито). Тільки відображення — БД не змінюється
- `DATABASE_URL` — Neon Postgres
- `TELEGRAM_BOT_TOKEN` — **єдиний бот на все**: `@OBVCarServiceWork_bot` (id 8683695884, новий, чистий, без webhook — перевірено `getWebhookInfo` 2026-07-10). Обслуговує і бота звітності (`getUpdates`), і вихідні алерти інжесту (`sendMessage`) — це не конфліктує. `@obvcarservicebot` (id 8979148940) більше **НЕ** використовується (його webhook належить CarBook — `api.carbook.pro/api/telegram/webhook/7971`)
- `TELEGRAM_CHAT_ID` — owner chat: отримувач алертів + дефолт для звітів. **Тут це ГРУПОВИЙ чат** (`-5426146652`), тому для ролей не годиться (ролі — по user-id). `TELEGRAM_ADMIN_CHAT_ID` — опційний окремий чат для алертів. `BOT_REPORT_CHAT_ID` (куди йдуть авто-звіти, тут = user `429132087`), `BOT_REPORT_TIMES` (`13:00,19:30`)
- `BOT_ALLOWED_CHAT_IDS` — **більше НЕ allowlist доступу**; тепер лише **bootstrap-сід директорів** на старті (`seedDirectors()` заносить ці user-id як `director` у `bot_users`, щоб не залочитись). Після першого запуску доступ керується через блок «Ролі» в боті; env-змінну можна не чіпати. Негативні (групові) id ігноруються при сідінгу
- `OPERATOR_ALIASES` — (бот, опційно) пари `номер=Ім'я` для показу сирого `manager_name` під людським іменем (див. вище)
- ⚠️ **Бот може писати юзеру лише після того, як той першим натиснув Start** — власник має один раз відкрити `@OBVCarServiceWork_bot` і написати йому, інакше авто-звіти/алерти не дійдуть (403 від Telegram)

## Деплой

Детальна інструкція — `DEPLOY.md`. Коротко: GitHub-репо (`TimohaVysochynskyi/BinotelCallAnalyzer`) підключений до Render, автобілд з `Dockerfile` при пуші в `main`. **Два сервіси з одного образу**: `binotel-poller` (Cron Job, `node src/jobs/index.js`) і `binotel-bot` (Background Worker, Docker Command `node src/bot/index.js`). Frankfurt (регіон Neon). Cron — Starter; Worker — постійний план (~$7/міс).

## Поточний статус / відкриті питання

- **Google Sheets** — ✅ видалено повністю (файл, залежність `googleapis`, env). У проєкті Sheets більше немає взагалі.
- **Звітність в інжесті** — ✅ видалено (`generateReport.js`, старий `pdfReport.js`, `sendDocument`, розклад `REPORT_HOURS`). Логіка звітів переїхала в бот. Google Sheets вирізано назавжди. **PDF повернувся в бот** (`src/bot/pdfReport.js`, `pdfkit` + `dejavu-fonts-ttf`): авто- і ручні звіти тепер PDF (одна сторінка на менеджера), а не текстове повідомлення на 4 частини
- **Джерело операторів = Binotel** — ✅ таблицю `managers` прибрано (клієнт керує операторами в Binotel). Стата/архів/звіти групуються по `manager_name` (ім'я від Binotel). Особисті 903/904/905 → ім'я з `employeeData.name`; спільні 901/902 → AI обирає з динамічного roster'а (імена, що Binotel уже давав). Це полагодило порожні стату/архів (раніше групувалось по `manager_id`, який був `NULL` у 100% дзвінків)
- **Ідентифікація 901/902** — ✅ обов'язкова, лишилась: `identifyManager.js` (enum-вибір з roster). Якість залежить від того, чи оператори представляються (клієнт це організує)
- **Telegram-бот звітності** — ✅ реалізовано (`src/bot/`, grammy, єдиний бот `@OBVCarServiceWork_bot` через `TELEGRAM_BOT_TOKEN`): авто-звіти 13:00/19:30, статистика менеджера (день/тиждень/місяць/квартал) + нотатки, архів розмов з прослуховуванням аудіо. Хостинг — Render Background Worker (клієнт погодив ~$7/міс)
- **База знань (RAG)** — ✅ реалізовано (`bot/kb.js` + pgvector): завантаження PDF/DOCX/TXT боту, embeddings у Postgres, кнопка «📚 База знань» (`kb:ask`, `/ask`) → відповідь лише з посібників. Протестовано наскрізь 2026-07-10 (Neon має pgvector 0.8.1). Скановані PDF/зображення без текстового шару → потрібен OCR (не реалізовано). Клієнт дасть реальні файли — перевірити витяг тексту з його PDF
- **Мова транскрибації** — ✅ детекція spoken-vs-written + переписування (суржик = uk); фікс «українську записує як російську» (`core/transcribe.js`)
- **UX бота** — ✅ нативні команди (кнопка «Menu») `/menu /stats /archive /ask /files /report /prompt` + inline-меню в повідомленнях + inline «« Меню» скрізь. Постійну reply-клавіатуру **прибрано** (забагато UI-навантаження); `/start` чистить її в старих клієнтів (`remove_keyboard`). Нативне меню тепер покриває й «📚 Файли» (раніше бракувало — єдина розбіжність із inline-меню)
- **Індикатори прогресу** — ✅ `bot/ui.js: withProgress()` тримає Telegram-екшн («друкує…»/«надсилає аудіо») живим на довгих операціях — KB-завантаження, питання до бази, «Звіт зараз», прослуховування аудіо (екшн Telegram гасне за ~5с → перевідсилка кожні 4с). Опційний `{ notice }` додатково шле постійне текст-повідомлення («⏳ Бот обробляє запит, це може зайняти деякий час…») і **видаляє** його у `finally` по завершенні — увімкнено для питань до бази знань і «Звіт зараз» (typing-екшн сам по собі малопомітний і гасне між тіками)
- **Фокус навігації / стійкий рендер** — ✅ `bot/ui.js: showScreen()` (замінив `safeEdit`) — модель «активного екрана» (див. «Навігація»): меню завжди в найнижчому повідомленні (edit-in-place коли воно внизу, інакше resend+delete старого), + Markdown→plain fallback, щоб крива розмітка (імена/нотатки з `_ * [`) не давала «мертву кнопку». Виправляє накладання тем: клік по меню, яке «поїхало» вгору під контент, більше не міняє щось угорі — свіже меню зʼявляється внизу, у фокусі
- **Промпт аналізу ефективності** — ✅ редагується прямо в боті через `/prompt` (переглянути / змінити / скинути до стандартного), зберігається в `app_state.analyze_prompt`; `analyze.js: getAnalyzePrompt()` бере його або `DEFAULT_ANALYZE_PROMPT`. Використовується в оцінці роботи операторів (авто-звіти + «Звіт зараз»). Клієнт може вписати свій текст без деплою — дефолт лишається запасним
- **Система ролей / доступ** — ✅ реалізовано (`bot/access.js` + `bot/roles.js` + таблиця `bot_users`). 4 ролі (director/marketer = повний admin-доступ; manager = звіт по собі + телефон + база знань; mechanic = лише база знань). Доступ і меню — по ролі з БД (не env). Блок «👥 Ролі» (admin): додати людину через `request_users` (обрати з контактів, миттєвий `user_id`) / контакт / телефон (pending→active за суфіксом телефону на `/start`); менеджеру лінкується `operator_name` з roster. `BOT_ALLOWED_CHAT_IDS` → лише bootstrap-сід директорів. Протестовано: DB-шар проти живої Neon (CRUD + activate-by-phone), матриця дозволів, реєстрація хендлерів, побудова `request_users`-клавіатури. ⚠️ Особисті авто-звіти менеджерам неможливі (бот не пише першим до Start) — «Мій звіт» тягнеться інтерактивно
- **Backfill переатрибуції** — ✅ є `npm run reattribute:shared` (переганяє старі 901/902 з голим номером через AI по збережених транскриптах). Прогнано 2026-07-10: 3/7 привʼязано до людей, решта без впевненого збігу (оператори тоді ще не представлялись)
