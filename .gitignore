node_modules
.env

# Local archive of call recordings (src/core/audioStore.js). Lives on the VPS, never in git.
data/

# Згенерований звіт за весь період: містить імена й телефони клієнтів,
# тому лежить тільки локально і в git не потрапляє.
report.html
report_*.html
report*.json
.vscode/
